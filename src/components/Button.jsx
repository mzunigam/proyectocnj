
export const Button = (props) => {

    // props.background = props.background || 'bg-blue-600';
    // props.color = props.color || 'text-white';
    // props.rounded = props.rounded || 'rounded';
    // props.padding = props.padding || 'px-3 py-3';
    // props.font = props.font || 'text-[1.5rem] font-bold';
    // props.text = props.text || 'Button';

    return (
        <button>Agregar</button>
    )
}

